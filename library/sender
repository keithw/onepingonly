#!/usr/bin/python

import audio_channel

BITS = [ 1, 0.5, 0, -0.5, -1 ]
BIT_LENGTH = 500

samples = []

for bit in BITS:
    for i in range( BIT_LENGTH ):
        samples.append( bit )

while True:
    audio_channel.send( samples )
